# Backend Src Directory

This directory contains the Express application source code.

## Structure

- **`config/`**: Contains database connections (Prisma client config).
- **`middleware/`**: Custom Express middlewares for JWT validation, schema checks, and errors.
- **`routes/`**: Handles path endpoints for auth, menu, and orders.
- **`index.js`**: Server app startup script.
